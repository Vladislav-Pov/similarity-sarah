diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
index fd7c6f0..fdaf5d4 100644
--- a/configs/algorithm/batched_nfg_sarah.yaml
+++ b/configs/algorithm/batched_nfg_sarah.yaml
@@ -3,7 +3,7 @@ name: batched_nfg_sarah
 num_clients: 10
 batch_size_clients: 1
 theta: 0.01
-num_epochs: 75
+num_epochs: 100
 
 # Inexact proximal solver hyper-parameters
 prox_num_steps: 8 # SGD steps per prox call
diff --git a/configs/config.yaml b/configs/config.yaml
index fa6deef..2ecb237 100644
--- a/configs/config.yaml
+++ b/configs/config.yaml
@@ -1,7 +1,7 @@
 defaults:
   - algorithm: batched_nfg_sarah
   - data: cifar10
-  - model: simple_cnn
+  - model: resnet18_32x32
   - partition: uniform
   - runtime: default
   - search: none
diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
index 9539d5b..68ee534 100644
--- a/configs/runtime/default.yaml
+++ b/configs/runtime/default.yaml
@@ -5,9 +5,9 @@ num_workers: 2
 # Minibatch size for training loaders. ``batch_size`` is the default for both roles.
 batch_size: 128
 # Server partition (objective f₁): override to differ from clients, e.g. smaller server shard.
-batch_size_server: ${batch_size}
+batch_size_server: ${.batch_size}
 # Client partitions: minibatch for each client's local DataLoader.
-batch_size_data_clients: ${batch_size}
+batch_size_data_clients: ${.batch_size}
 
 wandb:
   enabled: false
diff --git a/configs/search/optuna.yaml b/configs/search/optuna.yaml
index 14b0f98..6da7381 100644
--- a/configs/search/optuna.yaml
+++ b/configs/search/optuna.yaml
@@ -1,18 +1,18 @@
 enabled: true
 method: optuna
-max_epochs: 150
+max_epochs: 100
 num_trials: 1
 wandb_project: similarity-sarah
 wandb_group: "optuna-${now:%Y%m%d-%H%M%S}"
 
 shared:
   batch_size_clients: [1]
-  num_epochs: 150
+  num_epochs: 100
 
-distributed_sarah:
-  lr: [0.01]
+# distributed_sarah:
+#   lr: [0.01]
 
 batched_nfg_sarah:
   theta: [0.02]
   prox_lr: [0.01]
-  prox_num_steps: [18]
+  prox_num_steps: [12]
diff --git a/similarity_sarah/models/__init__.py b/similarity_sarah/models/__init__.py
index 87a4cc7..efd43dc 100644
--- a/similarity_sarah/models/__init__.py
+++ b/similarity_sarah/models/__init__.py
@@ -1,3 +1,4 @@
 from similarity_sarah.models.simple_cnn import SimpleCNN
+from similarity_sarah.models.resnet import ResNet18_32x32
 
-__all__ = ["SimpleCNN"]
+__all__ = ["SimpleCNN", "ResNet18_32x32"]
diff --git a/similarity_sarah/runtime/search.py b/similarity_sarah/runtime/search.py
index 0bc4b40..7d3409c 100644
--- a/similarity_sarah/runtime/search.py
+++ b/similarity_sarah/runtime/search.py
@@ -76,8 +76,8 @@ class GridSearch:
 
     def _iter_trials(self) -> Iterator[dict[str, object]]:
         shared_grid = _grid(_coerce_dict(getattr(self.cfg, "shared", None)))
-        dist_grid = _grid(_coerce_dict(self.cfg.distributed_sarah))
-        batched_grid = _grid(_coerce_dict(self.cfg.batched_nfg_sarah))
+        dist_grid = _grid(_coerce_dict(getattr(self.cfg, "distributed_sarah", None)))
+        batched_grid = _grid(_coerce_dict(getattr(self.cfg, "batched_nfg_sarah", None)))
 
         for shared_params in shared_grid:
             for dist_params in dist_grid:
@@ -199,14 +199,18 @@ def _sample_optuna_params(
             values_list,
         )
 
-    for name, values in _coerce_dict(search_cfg.distributed_sarah).items():
+    for name, values in _coerce_dict(
+        getattr(search_cfg, "distributed_sarah", None),
+    ).items():
         values_list = _as_list(values)
         params[f"distributed_sarah.{name}"] = trial.suggest_categorical(
             f"distributed_sarah.{name}",
             values_list,
         )
 
-    for name, values in _coerce_dict(search_cfg.batched_nfg_sarah).items():
+    for name, values in _coerce_dict(
+        getattr(search_cfg, "batched_nfg_sarah", None),
+    ).items():
         values_list = _as_list(values)
         params[f"batched_nfg_sarah.{name}"] = trial.suggest_categorical(
             f"batched_nfg_sarah.{name}",
diff --git a/wandb/latest-run b/wandb/latest-run
index 85079fb..de68e31 120000
--- a/wandb/latest-run
+++ b/wandb/latest-run
@@ -1 +1 @@
-run-20260414_093222-lt86lqrz
\ No newline at end of file
+run-20260417_195732-3eclkrjt
\ No newline at end of file
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/code/main.py b/wandb/run-20260411_204150-0g0fu1j7/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/config.yaml b/wandb/run-20260411_204150-0g0fu1j7/files/config.yaml
deleted file mode 100644
index c59bd4d..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/config.yaml
+++ /dev/null
@@ -1,181 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            eq92camkt42adr6nbuo8pi7uhycwlv0k:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115896963072"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T17:41:50.086632Z"
-                writerId: eq92camkt42adr6nbuo8pi7uhycwlv0k
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-204148
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr:
-            - 0.005
-            - 0.01
-        batched_nfg_sarah.prox_num_steps:
-            - 5
-            - 8
-            - 12
-        batched_nfg_sarah.theta:
-            - 0.005
-            - 0.01
-            - 0.02
-        distributed_sarah.lr:
-            - 0.005
-            - 0.01
-            - 0.02
-        shared.batch_size_clients:
-            - 2
-            - 4
-        shared.num_epochs: 75
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/diff.patch b/wandb/run-20260411_204150-0g0fu1j7/files/diff.patch
deleted file mode 100644
index 3cbde3a..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/diff.patch
+++ /dev/null
@@ -1,317 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..16c09f2 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -5,6 +5,8 @@ from __future__ import annotations
- import logging
- import time
- 
-+from omegaconf import OmegaConf
-+
- import torch
- from omegaconf import DictConfig
- from torch.utils.data import DataLoader
-@@ -16,6 +18,7 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
-@@ -31,9 +34,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +102,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +138,104 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = f"trial-{trial_id}"
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+        )
-+
-+        try:
-+            summaries = {}
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=0,
-+                )
-+                summaries[algo_name] = summary
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +255,71 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260411_204150-0g0fu1j7/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 3cbde3a..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,317 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..16c09f2 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -5,6 +5,8 @@ from __future__ import annotations
- import logging
- import time
- 
-+from omegaconf import OmegaConf
-+
- import torch
- from omegaconf import DictConfig
- from torch.utils.data import DataLoader
-@@ -16,6 +18,7 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
-@@ -31,9 +34,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +102,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +138,104 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = f"trial-{trial_id}"
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+        )
-+
-+        try:
-+            summaries = {}
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=0,
-+                )
-+                summaries[algo_name] = summary
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +255,71 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/requirements.txt b/wandb/run-20260411_204150-0g0fu1j7/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/wandb-metadata.json b/wandb/run-20260411_204150-0g0fu1j7/files/wandb-metadata.json
deleted file mode 100644
index 5cac8ae..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T17:41:50.086632Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115896963072"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "eq92camkt42adr6nbuo8pi7uhycwlv0k"
-}
\ No newline at end of file
diff --git a/wandb/run-20260411_204150-0g0fu1j7/files/wandb-summary.json b/wandb/run-20260411_204150-0g0fu1j7/files/wandb-summary.json
deleted file mode 100644
index 7b4fdaa..0000000
--- a/wandb/run-20260411_204150-0g0fu1j7/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"_wandb":{"runtime":1},"_runtime":1}
\ No newline at end of file
diff --git a/wandb/run-20260411_204150-0g0fu1j7/run-0g0fu1j7.wandb b/wandb/run-20260411_204150-0g0fu1j7/run-0g0fu1j7.wandb
deleted file mode 100644
index 44d33fb..0000000
Binary files a/wandb/run-20260411_204150-0g0fu1j7/run-0g0fu1j7.wandb and /dev/null differ
diff --git a/wandb/run-20260411_204653-o0tyrm46/files/code/main.py b/wandb/run-20260411_204653-o0tyrm46/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260411_204653-o0tyrm46/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260411_204653-o0tyrm46/files/diff.patch b/wandb/run-20260411_204653-o0tyrm46/files/diff.patch
deleted file mode 100644
index 3cbde3a..0000000
--- a/wandb/run-20260411_204653-o0tyrm46/files/diff.patch
+++ /dev/null
@@ -1,317 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..16c09f2 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -5,6 +5,8 @@ from __future__ import annotations
- import logging
- import time
- 
-+from omegaconf import OmegaConf
-+
- import torch
- from omegaconf import DictConfig
- from torch.utils.data import DataLoader
-@@ -16,6 +18,7 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
-@@ -31,9 +34,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +102,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +138,104 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = f"trial-{trial_id}"
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+        )
-+
-+        try:
-+            summaries = {}
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=0,
-+                )
-+                summaries[algo_name] = summary
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +255,71 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_204653-o0tyrm46/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260411_204653-o0tyrm46/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 3cbde3a..0000000
--- a/wandb/run-20260411_204653-o0tyrm46/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,317 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..16c09f2 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -5,6 +5,8 @@ from __future__ import annotations
- import logging
- import time
- 
-+from omegaconf import OmegaConf
-+
- import torch
- from omegaconf import DictConfig
- from torch.utils.data import DataLoader
-@@ -16,6 +18,7 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
-@@ -31,9 +34,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +102,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +138,104 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = f"trial-{trial_id}"
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+        )
-+
-+        try:
-+            summaries = {}
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=0,
-+                )
-+                summaries[algo_name] = summary
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +255,71 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_204653-o0tyrm46/files/requirements.txt b/wandb/run-20260411_204653-o0tyrm46/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260411_204653-o0tyrm46/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260411_204653-o0tyrm46/files/wandb-metadata.json b/wandb/run-20260411_204653-o0tyrm46/files/wandb-metadata.json
deleted file mode 100644
index 248031b..0000000
--- a/wandb/run-20260411_204653-o0tyrm46/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T17:46:53.239999Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115906134016"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "5698f33kzuya89a6zsrscclo0dle7xj8"
-}
\ No newline at end of file
diff --git a/wandb/run-20260411_204653-o0tyrm46/run-o0tyrm46.wandb b/wandb/run-20260411_204653-o0tyrm46/run-o0tyrm46.wandb
deleted file mode 100644
index 8f8d673..0000000
Binary files a/wandb/run-20260411_204653-o0tyrm46/run-o0tyrm46.wandb and /dev/null differ
diff --git a/wandb/run-20260411_214116-pfdby26h/files/code/main.py b/wandb/run-20260411_214116-pfdby26h/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260411_214116-pfdby26h/files/config.yaml b/wandb/run-20260411_214116-pfdby26h/files/config.yaml
deleted file mode 100644
index 76f13d9..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            3w3frqturx7rlivn87hhnlts4nh9pq5s:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116047372288"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T18:41:16.007248Z"
-                writerId: 3w3frqturx7rlivn87hhnlts4nh9pq5s
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.005
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 4
-        shared.num_epochs: 75
diff --git a/wandb/run-20260411_214116-pfdby26h/files/diff.patch b/wandb/run-20260411_214116-pfdby26h/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_214116-pfdby26h/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260411_214116-pfdby26h/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_214116-pfdby26h/files/requirements.txt b/wandb/run-20260411_214116-pfdby26h/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260411_214116-pfdby26h/files/wandb-metadata.json b/wandb/run-20260411_214116-pfdby26h/files/wandb-metadata.json
deleted file mode 100644
index e422180..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T18:41:16.007248Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116047372288"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "3w3frqturx7rlivn87hhnlts4nh9pq5s"
-}
\ No newline at end of file
diff --git a/wandb/run-20260411_214116-pfdby26h/files/wandb-summary.json b/wandb/run-20260411_214116-pfdby26h/files/wandb-summary.json
deleted file mode 100644
index 4ec7754..0000000
--- a/wandb/run-20260411_214116-pfdby26h/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"_runtime":4412.567210349,"distributed_sarah/test/accuracy":0.4014,"distributed_sarah/val/loss":1.7353962825775147,"batched_nfg_sarah/val/loss":2.1400632789611818,"batched_nfg_sarah/test/loss":2.133059660720825,"distributed_sarah/best_val_accuracy":0.3848,"batched_nfg_sarah/test/accuracy":0.2685,"batched_nfg_sarah/last_val_accuracy":0.2624,"batched_nfg_sarah/last_val_loss":2.1400632789611818,"distributed_sarah/test/loss":1.7179293561935425,"batched_nfg_sarah/best_val_accuracy":0.2748,"distributed_sarah/val/accuracy":0.384,"_timestamp":1.7759372894537296e+09,"batched_nfg_sarah/best_val_loss":2.1400632789611818,"_wandb":{"runtime":4412},"distributed_sarah/last_val_loss":1.7353962825775147,"distributed_sarah/best_val_loss":1.7353962825775147,"_step":150,"batched_nfg_sarah/val/accuracy":0.2624,"distributed_sarah/last_val_accuracy":0.384}
\ No newline at end of file
diff --git a/wandb/run-20260411_214116-pfdby26h/run-pfdby26h.wandb b/wandb/run-20260411_214116-pfdby26h/run-pfdby26h.wandb
deleted file mode 100644
index 176c6d9..0000000
Binary files a/wandb/run-20260411_214116-pfdby26h/run-pfdby26h.wandb and /dev/null differ
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/code/main.py b/wandb/run-20260411_225449-qlsh0dq8/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/config.yaml b/wandb/run-20260411_225449-qlsh0dq8/files/config.yaml
deleted file mode 100644
index 5ceed9f..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            7o63rc88kxwuxyxx0ztwh3epp2aylvv1:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116036374528"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T19:54:49.804082Z"
-                writerId: 7o63rc88kxwuxyxx0ztwh3epp2aylvv1
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 5
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.005
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/diff.patch b/wandb/run-20260411_225449-qlsh0dq8/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260411_225449-qlsh0dq8/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/requirements.txt b/wandb/run-20260411_225449-qlsh0dq8/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/wandb-metadata.json b/wandb/run-20260411_225449-qlsh0dq8/files/wandb-metadata.json
deleted file mode 100644
index f11e9c2..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T19:54:49.804082Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116036374528"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "7o63rc88kxwuxyxx0ztwh3epp2aylvv1"
-}
\ No newline at end of file
diff --git a/wandb/run-20260411_225449-qlsh0dq8/files/wandb-summary.json b/wandb/run-20260411_225449-qlsh0dq8/files/wandb-summary.json
deleted file mode 100644
index 85105b0..0000000
--- a/wandb/run-20260411_225449-qlsh0dq8/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/best_val_accuracy":0.3098,"batched_nfg_sarah/test/accuracy":0.3937,"batched_nfg_sarah/last_val_accuracy":0.3892,"distributed_sarah/last_val_loss":1.9950486890792847,"batched_nfg_sarah/best_val_loss":1.7398205268859863,"_runtime":4797.658131442,"distributed_sarah/val/accuracy":0.3098,"distributed_sarah/last_val_accuracy":0.3098,"distributed_sarah/val/loss":1.9950486890792847,"batched_nfg_sarah/last_val_loss":1.7398205268859863,"distributed_sarah/best_val_loss":1.9950486890792847,"batched_nfg_sarah/val/accuracy":0.3892,"_timestamp":1.7759420879800115e+09,"distributed_sarah/test/accuracy":0.3154,"batched_nfg_sarah/val/loss":1.7398205268859863,"_wandb":{"runtime":4797},"distributed_sarah/test/loss":1.9798244110107421,"_step":150,"batched_nfg_sarah/best_val_accuracy":0.3892,"batched_nfg_sarah/test/loss":1.7219122108459473}
\ No newline at end of file
diff --git a/wandb/run-20260411_225449-qlsh0dq8/run-qlsh0dq8.wandb b/wandb/run-20260411_225449-qlsh0dq8/run-qlsh0dq8.wandb
deleted file mode 100644
index 6b6e271..0000000
Binary files a/wandb/run-20260411_225449-qlsh0dq8/run-qlsh0dq8.wandb and /dev/null differ
diff --git a/wandb/run-20260412_001448-z7zudxna/files/code/main.py b/wandb/run-20260412_001448-z7zudxna/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_001448-z7zudxna/files/config.yaml b/wandb/run-20260412_001448-z7zudxna/files/config.yaml
deleted file mode 100644
index d7bb5d6..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            65r2y5gqa3qcg25yjkjt3o2xo2ik6nhp:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115971350528"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T21:14:48.368154Z"
-                writerId: 65r2y5gqa3qcg25yjkjt3o2xo2ik6nhp
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.02
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_001448-z7zudxna/files/diff.patch b/wandb/run-20260412_001448-z7zudxna/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_001448-z7zudxna/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_001448-z7zudxna/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_001448-z7zudxna/files/requirements.txt b/wandb/run-20260412_001448-z7zudxna/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_001448-z7zudxna/files/wandb-metadata.json b/wandb/run-20260412_001448-z7zudxna/files/wandb-metadata.json
deleted file mode 100644
index b6e3820..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T21:14:48.368154Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115971350528"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "65r2y5gqa3qcg25yjkjt3o2xo2ik6nhp"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_001448-z7zudxna/files/wandb-summary.json b/wandb/run-20260412_001448-z7zudxna/files/wandb-summary.json
deleted file mode 100644
index 3599dab..0000000
--- a/wandb/run-20260412_001448-z7zudxna/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/loss":1.6685636812210083,"batched_nfg_sarah/test/loss":1.5575545505523682,"batched_nfg_sarah/test/accuracy":0.4544,"distributed_sarah/val/accuracy":0.4106,"_wandb":{"runtime":5018},"distributed_sarah/last_val_loss":1.6685636812210083,"batched_nfg_sarah/best_val_accuracy":0.4408,"batched_nfg_sarah/last_val_loss":1.576335962677002,"distributed_sarah/test/loss":1.648884990310669,"distributed_sarah/test/accuracy":0.4268,"_runtime":5018.43791399,"_timestamp":1.7759471073407815e+09,"distributed_sarah/best_val_accuracy":0.413,"distributed_sarah/best_val_loss":1.6685636812210083,"distributed_sarah/last_val_accuracy":0.4106,"_step":150,"batched_nfg_sarah/val/loss":1.576335962677002,"batched_nfg_sarah/val/accuracy":0.4408,"batched_nfg_sarah/last_val_accuracy":0.4408,"batched_nfg_sarah/best_val_loss":1.576335962677002}
\ No newline at end of file
diff --git a/wandb/run-20260412_001448-z7zudxna/run-z7zudxna.wandb b/wandb/run-20260412_001448-z7zudxna/run-z7zudxna.wandb
deleted file mode 100644
index 2d628d5..0000000
Binary files a/wandb/run-20260412_001448-z7zudxna/run-z7zudxna.wandb and /dev/null differ
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/code/main.py b/wandb/run-20260412_013827-riqtmw3z/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/config.yaml b/wandb/run-20260412_013827-riqtmw3z/files/config.yaml
deleted file mode 100644
index 18a4286..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            zkcsel8ldsq2iw951e9b6hl26ecstrdj:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116170522624"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T22:38:27.726051Z"
-                writerId: zkcsel8ldsq2iw951e9b6hl26ecstrdj
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 5
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 4
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/diff.patch b/wandb/run-20260412_013827-riqtmw3z/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_013827-riqtmw3z/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/requirements.txt b/wandb/run-20260412_013827-riqtmw3z/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/wandb-metadata.json b/wandb/run-20260412_013827-riqtmw3z/files/wandb-metadata.json
deleted file mode 100644
index d40b7cd..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T22:38:27.726051Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116170522624"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "zkcsel8ldsq2iw951e9b6hl26ecstrdj"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_013827-riqtmw3z/files/wandb-summary.json b/wandb/run-20260412_013827-riqtmw3z/files/wandb-summary.json
deleted file mode 100644
index 369a3d6..0000000
--- a/wandb/run-20260412_013827-riqtmw3z/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/loss":1.769569416809082,"batched_nfg_sarah/best_val_loss":1.885361579322815,"distributed_sarah/last_val_accuracy":0.3748,"_timestamp":1.7759516930701904e+09,"batched_nfg_sarah/test/loss":1.870573572921753,"distributed_sarah/best_val_loss":1.7352870069503785,"distributed_sarah/test/loss":1.7526214221954346,"_step":150,"batched_nfg_sarah/best_val_accuracy":0.3444,"batched_nfg_sarah/val/loss":1.885361579322815,"_runtime":4584.803933421,"batched_nfg_sarah/test/accuracy":0.3427,"distributed_sarah/best_val_accuracy":0.3962,"batched_nfg_sarah/last_val_loss":1.885361579322815,"distributed_sarah/val/accuracy":0.3748,"batched_nfg_sarah/last_val_accuracy":0.3424,"_wandb":{"runtime":4584},"distributed_sarah/test/accuracy":0.3863,"distributed_sarah/last_val_loss":1.769569416809082,"batched_nfg_sarah/val/accuracy":0.3424}
\ No newline at end of file
diff --git a/wandb/run-20260412_013827-riqtmw3z/run-riqtmw3z.wandb b/wandb/run-20260412_013827-riqtmw3z/run-riqtmw3z.wandb
deleted file mode 100644
index a7bdf3c..0000000
Binary files a/wandb/run-20260412_013827-riqtmw3z/run-riqtmw3z.wandb and /dev/null differ
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/code/main.py b/wandb/run-20260412_025453-g2v4n3at/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/config.yaml b/wandb/run-20260412_025453-g2v4n3at/files/config.yaml
deleted file mode 100644
index 60bc9d0..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            5bmffoswwt24l3047bmdiri6cx4bume1:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116154634240"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-11T23:54:53.375478Z"
-                writerId: 5bmffoswwt24l3047bmdiri6cx4bume1
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/diff.patch b/wandb/run-20260412_025453-g2v4n3at/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_025453-g2v4n3at/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/requirements.txt b/wandb/run-20260412_025453-g2v4n3at/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/wandb-metadata.json b/wandb/run-20260412_025453-g2v4n3at/files/wandb-metadata.json
deleted file mode 100644
index e9c08f2..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-11T23:54:53.375478Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116154634240"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "5bmffoswwt24l3047bmdiri6cx4bume1"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_025453-g2v4n3at/files/wandb-summary.json b/wandb/run-20260412_025453-g2v4n3at/files/wandb-summary.json
deleted file mode 100644
index a629d2b..0000000
--- a/wandb/run-20260412_025453-g2v4n3at/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/best_val_accuracy":0.3768,"distributed_sarah/val/accuracy":0.4328,"batched_nfg_sarah/last_val_loss":1.7850384601593017,"_step":150,"distributed_sarah/test/loss":1.5985477600097657,"batched_nfg_sarah/test/loss":1.766313822746277,"_wandb":{"runtime":5029},"batched_nfg_sarah/val/loss":1.7850384601593017,"_timestamp":1.7759567239885044e+09,"distributed_sarah/last_val_loss":1.618150408935547,"_runtime":5029.925189387,"distributed_sarah/val/loss":1.618150408935547,"distributed_sarah/last_val_accuracy":0.4328,"batched_nfg_sarah/test/accuracy":0.386,"batched_nfg_sarah/val/accuracy":0.3768,"batched_nfg_sarah/best_val_loss":1.7850384601593017,"distributed_sarah/best_val_accuracy":0.4328,"distributed_sarah/test/accuracy":0.4421,"batched_nfg_sarah/last_val_accuracy":0.3768,"distributed_sarah/best_val_loss":1.618150408935547}
\ No newline at end of file
diff --git a/wandb/run-20260412_025453-g2v4n3at/run-g2v4n3at.wandb b/wandb/run-20260412_025453-g2v4n3at/run-g2v4n3at.wandb
deleted file mode 100644
index 4992aa8..0000000
Binary files a/wandb/run-20260412_025453-g2v4n3at/run-g2v4n3at.wandb and /dev/null differ
diff --git a/wandb/run-20260412_041844-g6oawur3/files/code/main.py b/wandb/run-20260412_041844-g6oawur3/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_041844-g6oawur3/files/config.yaml b/wandb/run-20260412_041844-g6oawur3/files/config.yaml
deleted file mode 100644
index 4a3b62b..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            n1a926kqbcxt5abt3t0vxsp5cctr748t:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116193898496"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T01:18:44.389798Z"
-                writerId: n1a926kqbcxt5abt3t0vxsp5cctr748t
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 5
-        batched_nfg_sarah.theta: 0.005
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 4
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_041844-g6oawur3/files/diff.patch b/wandb/run-20260412_041844-g6oawur3/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_041844-g6oawur3/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_041844-g6oawur3/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_041844-g6oawur3/files/requirements.txt b/wandb/run-20260412_041844-g6oawur3/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_041844-g6oawur3/files/wandb-metadata.json b/wandb/run-20260412_041844-g6oawur3/files/wandb-metadata.json
deleted file mode 100644
index d85d718..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T01:18:44.389798Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116193898496"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "n1a926kqbcxt5abt3t0vxsp5cctr748t"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_041844-g6oawur3/files/wandb-summary.json b/wandb/run-20260412_041844-g6oawur3/files/wandb-summary.json
deleted file mode 100644
index 046a81a..0000000
--- a/wandb/run-20260412_041844-g6oawur3/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/test/loss":1.9452100818634033,"batched_nfg_sarah/val/loss":1.9608603218078613,"distributed_sarah/last_val_accuracy":0.3772,"_wandb":{"runtime":4447},"distributed_sarah/best_val_accuracy":0.3772,"_step":150,"batched_nfg_sarah/best_val_loss":1.955233673286438,"batched_nfg_sarah/last_val_loss":1.9608603218078613,"batched_nfg_sarah/test/accuracy":0.3146,"batched_nfg_sarah/last_val_accuracy":0.3152,"distributed_sarah/val/loss":1.7690756332397461,"batched_nfg_sarah/val/accuracy":0.3152,"_timestamp":1.7759611724457965e+09,"distributed_sarah/test/loss":1.7478350343704223,"_runtime":4447.399690971,"distributed_sarah/val/accuracy":0.3772,"batched_nfg_sarah/best_val_accuracy":0.3158,"distributed_sarah/test/accuracy":0.3916,"distributed_sarah/best_val_loss":1.7690756332397461,"distributed_sarah/last_val_loss":1.7690756332397461}
\ No newline at end of file
diff --git a/wandb/run-20260412_041844-g6oawur3/run-g6oawur3.wandb b/wandb/run-20260412_041844-g6oawur3/run-g6oawur3.wandb
deleted file mode 100644
index 49f4563..0000000
Binary files a/wandb/run-20260412_041844-g6oawur3/run-g6oawur3.wandb and /dev/null differ
diff --git a/wandb/run-20260412_053252-nle24yc0/files/code/main.py b/wandb/run-20260412_053252-nle24yc0/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_053252-nle24yc0/files/config.yaml b/wandb/run-20260412_053252-nle24yc0/files/config.yaml
deleted file mode 100644
index b9aebad..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            gbfjnpd3l86hvx425eo06xq836alxlol:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116371615744"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T02:32:52.718272Z"
-                writerId: gbfjnpd3l86hvx425eo06xq836alxlol
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 5
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_053252-nle24yc0/files/diff.patch b/wandb/run-20260412_053252-nle24yc0/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_053252-nle24yc0/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_053252-nle24yc0/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_053252-nle24yc0/files/requirements.txt b/wandb/run-20260412_053252-nle24yc0/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_053252-nle24yc0/files/wandb-metadata.json b/wandb/run-20260412_053252-nle24yc0/files/wandb-metadata.json
deleted file mode 100644
index 87fcc42..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T02:32:52.718272Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116371615744"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "gbfjnpd3l86hvx425eo06xq836alxlol"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_053252-nle24yc0/files/wandb-summary.json b/wandb/run-20260412_053252-nle24yc0/files/wandb-summary.json
deleted file mode 100644
index 86aa0ae..0000000
--- a/wandb/run-20260412_053252-nle24yc0/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/accuracy":0.4374,"distributed_sarah/best_val_loss":1.620575426864624,"_timestamp":1.775966107371532e+09,"batched_nfg_sarah/val/loss":1.7678633317947388,"batched_nfg_sarah/last_val_loss":1.7678633317947388,"_wandb":{"runtime":4934},"distributed_sarah/best_val_accuracy":0.4374,"batched_nfg_sarah/test/loss":1.753616562461853,"_runtime":4934.360247335,"distributed_sarah/test/accuracy":0.442,"distributed_sarah/test/loss":1.6028614768981932,"batched_nfg_sarah/best_val_loss":1.7678633317947388,"batched_nfg_sarah/last_val_accuracy":0.3782,"batched_nfg_sarah/test/accuracy":0.3874,"distributed_sarah/val/loss":1.6220454544067382,"batched_nfg_sarah/val/accuracy":0.3782,"distributed_sarah/last_val_loss":1.6220454544067382,"_step":150,"batched_nfg_sarah/best_val_accuracy":0.3792,"distributed_sarah/last_val_accuracy":0.4374}
\ No newline at end of file
diff --git a/wandb/run-20260412_053252-nle24yc0/run-nle24yc0.wandb b/wandb/run-20260412_053252-nle24yc0/run-nle24yc0.wandb
deleted file mode 100644
index 4c27ee8..0000000
Binary files a/wandb/run-20260412_053252-nle24yc0/run-nle24yc0.wandb and /dev/null differ
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/code/main.py b/wandb/run-20260412_065507-fclb5xv9/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/config.yaml b/wandb/run-20260412_065507-fclb5xv9/files/config.yaml
deleted file mode 100644
index a2470c7..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            xgzhfipsdynkfh00gdboue5y09s1x3pd:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116181364736"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T03:55:07.660474Z"
-                writerId: xgzhfipsdynkfh00gdboue5y09s1x3pd
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 5
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/diff.patch b/wandb/run-20260412_065507-fclb5xv9/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_065507-fclb5xv9/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/requirements.txt b/wandb/run-20260412_065507-fclb5xv9/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/wandb-metadata.json b/wandb/run-20260412_065507-fclb5xv9/files/wandb-metadata.json
deleted file mode 100644
index c319d73..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T03:55:07.660474Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116181364736"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "xgzhfipsdynkfh00gdboue5y09s1x3pd"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_065507-fclb5xv9/files/wandb-summary.json b/wandb/run-20260412_065507-fclb5xv9/files/wandb-summary.json
deleted file mode 100644
index 053d312..0000000
--- a/wandb/run-20260412_065507-fclb5xv9/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"_wandb":{"runtime":4838},"batched_nfg_sarah/last_val_loss":1.808119009590149,"distributed_sarah/last_val_loss":1.8232695667266845,"distributed_sarah/val/loss":1.8232695667266845,"distributed_sarah/best_val_loss":1.8232695667266845,"batched_nfg_sarah/test/loss":1.784810074043274,"batched_nfg_sarah/best_val_loss":1.8075804580688477,"distributed_sarah/test/accuracy":0.3732,"batched_nfg_sarah/test/accuracy":0.3745,"batched_nfg_sarah/best_val_accuracy":0.3698,"distributed_sarah/last_val_accuracy":0.361,"batched_nfg_sarah/val/accuracy":0.367,"distributed_sarah/val/accuracy":0.361,"distributed_sarah/test/loss":1.802610451889038,"_timestamp":1.7759709467278748e+09,"_step":150,"batched_nfg_sarah/val/loss":1.808119009590149,"batched_nfg_sarah/last_val_accuracy":0.367,"_runtime":4838.773224753,"distributed_sarah/best_val_accuracy":0.361}
\ No newline at end of file
diff --git a/wandb/run-20260412_065507-fclb5xv9/run-fclb5xv9.wandb b/wandb/run-20260412_065507-fclb5xv9/run-fclb5xv9.wandb
deleted file mode 100644
index c5b6a3f..0000000
Binary files a/wandb/run-20260412_065507-fclb5xv9/run-fclb5xv9.wandb and /dev/null differ
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/code/main.py b/wandb/run-20260412_081547-hlhtjh22/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/config.yaml b/wandb/run-20260412_081547-hlhtjh22/files/config.yaml
deleted file mode 100644
index f18183f..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            f5ttim5n76ucfm4na7xvtmuo40syjngv:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116191793152"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T05:15:47.002138Z"
-                writerId: f5ttim5n76ucfm4na7xvtmuo40syjngv
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 12
-        batched_nfg_sarah.theta: 0.02
-        distributed_sarah.lr: 0.005
-        shared.batch_size_clients: 2
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/diff.patch b/wandb/run-20260412_081547-hlhtjh22/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_081547-hlhtjh22/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/requirements.txt b/wandb/run-20260412_081547-hlhtjh22/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/wandb-metadata.json b/wandb/run-20260412_081547-hlhtjh22/files/wandb-metadata.json
deleted file mode 100644
index 586aa02..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T05:15:47.002138Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116191793152"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "f5ttim5n76ucfm4na7xvtmuo40syjngv"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_081547-hlhtjh22/files/wandb-summary.json b/wandb/run-20260412_081547-hlhtjh22/files/wandb-summary.json
deleted file mode 100644
index 75d6a30..0000000
--- a/wandb/run-20260412_081547-hlhtjh22/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/val/accuracy":0.4538,"distributed_sarah/best_val_accuracy":0.2824,"_wandb":{"runtime":4856},"distributed_sarah/val/loss":2.0619701515197755,"_timestamp":1.7759758035827575e+09,"_runtime":4856.289808557,"batched_nfg_sarah/best_val_accuracy":0.471,"distributed_sarah/val/accuracy":0.2824,"distributed_sarah/test/loss":2.048565663909912,"_step":150,"distributed_sarah/last_val_accuracy":0.2824,"batched_nfg_sarah/last_val_accuracy":0.4538,"batched_nfg_sarah/test/accuracy":0.4723,"distributed_sarah/best_val_loss":2.0619701515197755,"batched_nfg_sarah/last_val_loss":1.5128568929672241,"batched_nfg_sarah/val/loss":1.5128568929672241,"distributed_sarah/test/accuracy":0.2906,"distributed_sarah/last_val_loss":2.0619701515197755,"batched_nfg_sarah/best_val_loss":1.4934694690704347,"batched_nfg_sarah/test/loss":1.492619234085083}
\ No newline at end of file
diff --git a/wandb/run-20260412_081547-hlhtjh22/run-hlhtjh22.wandb b/wandb/run-20260412_081547-hlhtjh22/run-hlhtjh22.wandb
deleted file mode 100644
index 137b99f..0000000
Binary files a/wandb/run-20260412_081547-hlhtjh22/run-hlhtjh22.wandb and /dev/null differ
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/code/main.py b/wandb/run-20260412_093643-qu4vd7v1/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/config.yaml b/wandb/run-20260412_093643-qu4vd7v1/files/config.yaml
deleted file mode 100644
index ddcf284..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/config.yaml
+++ /dev/null
@@ -1,170 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            acl8el19zhjb8azg6xagehrg2oclc981:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116304777216"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T06:36:43.833004Z"
-                writerId: acl8el19zhjb8azg6xagehrg2oclc981
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 5
-                    - 8
-                    - 12
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 75
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 2
-                    - 4
-                num_epochs: 75
-            wandb_group: optuna-20260411-214114
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 4
-        shared.num_epochs: 75
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/diff.patch b/wandb/run-20260412_093643-qu4vd7v1/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260412_093643-qu4vd7v1/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/requirements.txt b/wandb/run-20260412_093643-qu4vd7v1/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/wandb-metadata.json b/wandb/run-20260412_093643-qu4vd7v1/files/wandb-metadata.json
deleted file mode 100644
index 0b7a451..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T06:36:43.833004Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116304777216"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "acl8el19zhjb8azg6xagehrg2oclc981"
-}
\ No newline at end of file
diff --git a/wandb/run-20260412_093643-qu4vd7v1/files/wandb-summary.json b/wandb/run-20260412_093643-qu4vd7v1/files/wandb-summary.json
deleted file mode 100644
index 14bdd3d..0000000
--- a/wandb/run-20260412_093643-qu4vd7v1/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/last_val_accuracy":0.3192,"distributed_sarah/test/loss":1.9228876518249511,"batched_nfg_sarah/best_val_accuracy":0.3506,"distributed_sarah/val/accuracy":0.3192,"distributed_sarah/test/accuracy":0.3258,"_wandb":{"runtime":4304},"batched_nfg_sarah/last_val_loss":1.8646041746139526,"_runtime":4304.546335497,"batched_nfg_sarah/val/accuracy":0.3506,"distributed_sarah/val/loss":1.940133857154846,"distributed_sarah/last_val_loss":1.940133857154846,"_timestamp":1.7759801086698112e+09,"batched_nfg_sarah/test/accuracy":0.3497,"batched_nfg_sarah/test/loss":1.8530747554779052,"batched_nfg_sarah/best_val_loss":1.8646041746139526,"batched_nfg_sarah/last_val_accuracy":0.3506,"distributed_sarah/best_val_accuracy":0.3192,"batched_nfg_sarah/val/loss":1.8646041746139526,"distributed_sarah/best_val_loss":1.940133857154846,"_step":150}
\ No newline at end of file
diff --git a/wandb/run-20260412_093643-qu4vd7v1/run-qu4vd7v1.wandb b/wandb/run-20260412_093643-qu4vd7v1/run-qu4vd7v1.wandb
deleted file mode 100644
index d260e72..0000000
Binary files a/wandb/run-20260412_093643-qu4vd7v1/run-qu4vd7v1.wandb and /dev/null differ
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/code/main.py b/wandb/run-20260413_012706-5ba4kb9u/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/config.yaml b/wandb/run-20260413_012706-5ba4kb9u/files/config.yaml
deleted file mode 100644
index 04a9da6..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            1pzmnj9sn4sg34zk4a3vywl9001j2hf2:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116343599104"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-12T22:27:06.299589Z"
-                writerId: 1pzmnj9sn4sg34zk4a3vywl9001j2hf2
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 18
-        batched_nfg_sarah.theta: 0.02
-        distributed_sarah.lr: 0.005
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/diff.patch b/wandb/run-20260413_012706-5ba4kb9u/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_012706-5ba4kb9u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/requirements.txt b/wandb/run-20260413_012706-5ba4kb9u/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/wandb-metadata.json b/wandb/run-20260413_012706-5ba4kb9u/files/wandb-metadata.json
deleted file mode 100644
index 2e8b969..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-12T22:27:06.299589Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116343599104"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "1pzmnj9sn4sg34zk4a3vywl9001j2hf2"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_012706-5ba4kb9u/files/wandb-summary.json b/wandb/run-20260413_012706-5ba4kb9u/files/wandb-summary.json
deleted file mode 100644
index d17e87e..0000000
--- a/wandb/run-20260413_012706-5ba4kb9u/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/test/loss":1.1264312433242798,"batched_nfg_sarah/best_val_accuracy":0.6074,"distributed_sarah/last_val_accuracy":0.4362,"_wandb":{"runtime":12566},"batched_nfg_sarah/last_val_accuracy":0.5986,"distributed_sarah/last_val_loss":1.5857297115325928,"distributed_sarah/test/loss":1.5694488563537599,"distributed_sarah/best_val_loss":1.5857297115325928,"batched_nfg_sarah/test/accuracy":0.6032,"_step":300,"distributed_sarah/test/accuracy":0.4511,"batched_nfg_sarah/val/loss":1.1427293380737304,"_runtime":12566.116328433,"distributed_sarah/best_val_accuracy":0.4364,"batched_nfg_sarah/last_val_loss":1.1427293380737304,"distributed_sarah/val/accuracy":0.4362,"distributed_sarah/val/loss":1.5857297115325928,"batched_nfg_sarah/best_val_loss":1.1209988164901734,"_timestamp":1.7760453934121935e+09,"batched_nfg_sarah/val/accuracy":0.5986}
\ No newline at end of file
diff --git a/wandb/run-20260413_012706-5ba4kb9u/run-5ba4kb9u.wandb b/wandb/run-20260413_012706-5ba4kb9u/run-5ba4kb9u.wandb
deleted file mode 100644
index c334695..0000000
Binary files a/wandb/run-20260413_012706-5ba4kb9u/run-5ba4kb9u.wandb and /dev/null differ
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/code/main.py b/wandb/run-20260413_045634-svlgcc5u/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/config.yaml b/wandb/run-20260413_045634-svlgcc5u/files/config.yaml
deleted file mode 100644
index 38b655c..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            vlb785543trya5o16v137jbehwmlg5u2:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115890634752"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T01:56:34.006310Z"
-                writerId: vlb785543trya5o16v137jbehwmlg5u2
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 12
-        batched_nfg_sarah.theta: 0.005
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/diff.patch b/wandb/run-20260413_045634-svlgcc5u/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_045634-svlgcc5u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/requirements.txt b/wandb/run-20260413_045634-svlgcc5u/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/wandb-metadata.json b/wandb/run-20260413_045634-svlgcc5u/files/wandb-metadata.json
deleted file mode 100644
index 37c1729..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T01:56:34.006310Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115890634752"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "vlb785543trya5o16v137jbehwmlg5u2"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_045634-svlgcc5u/files/wandb-summary.json b/wandb/run-20260413_045634-svlgcc5u/files/wandb-summary.json
deleted file mode 100644
index 55319c9..0000000
--- a/wandb/run-20260413_045634-svlgcc5u/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/last_val_loss":1.3573465194702148,"_timestamp":1.776057808712916e+09,"_runtime":12413.840207039,"batched_nfg_sarah/last_val_accuracy":0.0976,"distributed_sarah/last_val_accuracy":0.5128,"_step":300,"_wandb":{"runtime":12413},"batched_nfg_sarah/val/loss":NaN,"distributed_sarah/best_val_accuracy":0.5172,"batched_nfg_sarah/test/loss":NaN,"distributed_sarah/val/loss":1.3573465194702148,"distributed_sarah/test/loss":1.3368852024078368,"distributed_sarah/best_val_loss":1.353948490333557,"batched_nfg_sarah/best_val_loss":2.3279623943328858,"distributed_sarah/val/accuracy":0.5128,"distributed_sarah/test/accuracy":0.5255,"batched_nfg_sarah/test/accuracy":0.1,"batched_nfg_sarah/val/accuracy":0.0976,"batched_nfg_sarah/best_val_accuracy":0.0976}
\ No newline at end of file
diff --git a/wandb/run-20260413_045634-svlgcc5u/run-svlgcc5u.wandb b/wandb/run-20260413_045634-svlgcc5u/run-svlgcc5u.wandb
deleted file mode 100644
index ca5abf2..0000000
Binary files a/wandb/run-20260413_045634-svlgcc5u/run-svlgcc5u.wandb and /dev/null differ
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/code/main.py b/wandb/run-20260413_082329-w6z2r54x/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/config.yaml b/wandb/run-20260413_082329-w6z2r54x/files/config.yaml
deleted file mode 100644
index a7daa80..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            09ucwwdvzp7mbklu7uirjbryvz299v8v:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115814592512"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T05:23:29.049950Z"
-                writerId: 09ucwwdvzp7mbklu7uirjbryvz299v8v
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/diff.patch b/wandb/run-20260413_082329-w6z2r54x/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_082329-w6z2r54x/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/requirements.txt b/wandb/run-20260413_082329-w6z2r54x/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/wandb-metadata.json b/wandb/run-20260413_082329-w6z2r54x/files/wandb-metadata.json
deleted file mode 100644
index 855be86..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T05:23:29.049950Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115814592512"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "09ucwwdvzp7mbklu7uirjbryvz299v8v"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_082329-w6z2r54x/files/wandb-summary.json b/wandb/run-20260413_082329-w6z2r54x/files/wandb-summary.json
deleted file mode 100644
index b5b81d1..0000000
--- a/wandb/run-20260413_082329-w6z2r54x/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/loss":1.19831683177948,"batched_nfg_sarah/best_val_accuracy":0.438,"distributed_sarah/best_val_loss":1.190827699279785,"_step":300,"distributed_sarah/test/accuracy":0.5839,"_timestamp":1.7760692900199487e+09,"distributed_sarah/test/loss":1.183574559020996,"distributed_sarah/last_val_accuracy":0.5804,"distributed_sarah/best_val_accuracy":0.5804,"batched_nfg_sarah/test/accuracy":0.1,"batched_nfg_sarah/test/loss":NaN,"batched_nfg_sarah/val/accuracy":0.0976,"_wandb":{"runtime":11480},"batched_nfg_sarah/val/loss":NaN,"batched_nfg_sarah/best_val_loss":1.5979410207748412,"_runtime":11480.677521594,"distributed_sarah/last_val_loss":1.19831683177948,"distributed_sarah/val/accuracy":0.5804,"batched_nfg_sarah/last_val_accuracy":0.0976}
\ No newline at end of file
diff --git a/wandb/run-20260413_082329-w6z2r54x/run-w6z2r54x.wandb b/wandb/run-20260413_082329-w6z2r54x/run-w6z2r54x.wandb
deleted file mode 100644
index 3979109..0000000
Binary files a/wandb/run-20260413_082329-w6z2r54x/run-w6z2r54x.wandb and /dev/null differ
diff --git a/wandb/run-20260413_113450-11qr7emn/files/code/main.py b/wandb/run-20260413_113450-11qr7emn/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_113450-11qr7emn/files/config.yaml b/wandb/run-20260413_113450-11qr7emn/files/config.yaml
deleted file mode 100644
index fd90a13..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            c4fgixpv7ys5qmbtfyiyiulgmn8yipo7:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116103901184"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T08:34:50.330802Z"
-                writerId: c4fgixpv7ys5qmbtfyiyiulgmn8yipo7
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 18
-        batched_nfg_sarah.theta: 0.005
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_113450-11qr7emn/files/diff.patch b/wandb/run-20260413_113450-11qr7emn/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_113450-11qr7emn/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_113450-11qr7emn/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_113450-11qr7emn/files/requirements.txt b/wandb/run-20260413_113450-11qr7emn/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_113450-11qr7emn/files/wandb-metadata.json b/wandb/run-20260413_113450-11qr7emn/files/wandb-metadata.json
deleted file mode 100644
index ed74553..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T08:34:50.330802Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116103901184"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "c4fgixpv7ys5qmbtfyiyiulgmn8yipo7"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_113450-11qr7emn/files/wandb-summary.json b/wandb/run-20260413_113450-11qr7emn/files/wandb-summary.json
deleted file mode 100644
index ffd9e09..0000000
--- a/wandb/run-20260413_113450-11qr7emn/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/best_val_accuracy":0.5868,"_runtime":12375.376881495,"distributed_sarah/test/loss":1.1689831275939941,"distributed_sarah/test/accuracy":0.5838,"batched_nfg_sarah/best_val_loss":2.3494920352935793,"distributed_sarah/val/loss":1.1819634899139404,"batched_nfg_sarah/best_val_accuracy":0.0976,"_wandb":{"runtime":12375},"distributed_sarah/best_val_loss":1.170562629699707,"batched_nfg_sarah/test/accuracy":0.1,"batched_nfg_sarah/last_val_accuracy":0.0976,"distributed_sarah/last_val_accuracy":0.5824,"_timestamp":1.7760816660180311e+09,"batched_nfg_sarah/test/loss":NaN,"distributed_sarah/val/accuracy":0.5824,"batched_nfg_sarah/val/loss":NaN,"_step":300,"distributed_sarah/last_val_loss":1.1819634899139404,"batched_nfg_sarah/val/accuracy":0.0976}
\ No newline at end of file
diff --git a/wandb/run-20260413_113450-11qr7emn/run-11qr7emn.wandb b/wandb/run-20260413_113450-11qr7emn/run-11qr7emn.wandb
deleted file mode 100644
index 4ad417c..0000000
Binary files a/wandb/run-20260413_113450-11qr7emn/run-11qr7emn.wandb and /dev/null differ
diff --git a/wandb/run-20260413_150106-hric55kt/files/code/main.py b/wandb/run-20260413_150106-hric55kt/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_150106-hric55kt/files/config.yaml b/wandb/run-20260413_150106-hric55kt/files/config.yaml
deleted file mode 100644
index cba10db..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            w6fwcp6usl4x1pagqa2el9tm4h64e7ll:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116238487552"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T12:01:06.378209Z"
-                writerId: w6fwcp6usl4x1pagqa2el9tm4h64e7ll
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 12
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_150106-hric55kt/files/diff.patch b/wandb/run-20260413_150106-hric55kt/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_150106-hric55kt/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_150106-hric55kt/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_150106-hric55kt/files/requirements.txt b/wandb/run-20260413_150106-hric55kt/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_150106-hric55kt/files/wandb-metadata.json b/wandb/run-20260413_150106-hric55kt/files/wandb-metadata.json
deleted file mode 100644
index b7380ff..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T12:01:06.378209Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116238487552"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "w6fwcp6usl4x1pagqa2el9tm4h64e7ll"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_150106-hric55kt/files/wandb-summary.json b/wandb/run-20260413_150106-hric55kt/files/wandb-summary.json
deleted file mode 100644
index 81288f5..0000000
--- a/wandb/run-20260413_150106-hric55kt/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/test/accuracy":0.1,"distributed_sarah/val/loss":1.4056696975708007,"batched_nfg_sarah/val/accuracy":0.0976,"_timestamp":1.7760969186078248e+09,"distributed_sarah/best_val_loss":1.3868948009490967,"batched_nfg_sarah/best_val_accuracy":0.4136,"distributed_sarah/test/accuracy":0.5114,"_wandb":{"runtime":15251},"batched_nfg_sarah/best_val_loss":1.657036935043335,"_runtime":15251.800690858,"distributed_sarah/val/accuracy":0.502,"distributed_sarah/last_val_accuracy":0.502,"batched_nfg_sarah/last_val_accuracy":0.0976,"distributed_sarah/test/loss":1.385262685394287,"_step":300,"batched_nfg_sarah/val/loss":NaN,"distributed_sarah/best_val_accuracy":0.5092,"batched_nfg_sarah/test/loss":NaN,"distributed_sarah/last_val_loss":1.4056696975708007}
\ No newline at end of file
diff --git a/wandb/run-20260413_150106-hric55kt/run-hric55kt.wandb b/wandb/run-20260413_150106-hric55kt/run-hric55kt.wandb
deleted file mode 100644
index 78af649..0000000
Binary files a/wandb/run-20260413_150106-hric55kt/run-hric55kt.wandb and /dev/null differ
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/code/main.py b/wandb/run-20260413_191519-n5cnqq0l/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/config.yaml b/wandb/run-20260413_191519-n5cnqq0l/files/config.yaml
deleted file mode 100644
index 908589e..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            xiqft9ys2eclrh0n3hiuvi9q69zctl1a:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116213493760"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T16:15:19.088258Z"
-                writerId: xiqft9ys2eclrh0n3hiuvi9q69zctl1a
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 8
-        batched_nfg_sarah.theta: 0.005
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/diff.patch b/wandb/run-20260413_191519-n5cnqq0l/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_191519-n5cnqq0l/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/requirements.txt b/wandb/run-20260413_191519-n5cnqq0l/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/wandb-metadata.json b/wandb/run-20260413_191519-n5cnqq0l/files/wandb-metadata.json
deleted file mode 100644
index 183da48..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T16:15:19.088258Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116213493760"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "xiqft9ys2eclrh0n3hiuvi9q69zctl1a"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_191519-n5cnqq0l/files/wandb-summary.json b/wandb/run-20260413_191519-n5cnqq0l/files/wandb-summary.json
deleted file mode 100644
index 4f1c964..0000000
--- a/wandb/run-20260413_191519-n5cnqq0l/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/last_val_loss":1.5362514652252197,"distributed_sarah/val/accuracy":0.513,"batched_nfg_sarah/last_val_accuracy":0.4438,"_step":300,"distributed_sarah/best_val_accuracy":0.5158,"batched_nfg_sarah/test/loss":1.5180950401306152,"batched_nfg_sarah/best_val_loss":1.5362514652252197,"distributed_sarah/test/accuracy":0.5162,"distributed_sarah/last_val_accuracy":0.513,"distributed_sarah/best_val_loss":1.3710181356430053,"batched_nfg_sarah/test/accuracy":0.4647,"_timestamp":1.7761108011065302e+09,"batched_nfg_sarah/val/accuracy":0.4438,"distributed_sarah/last_val_loss":1.3710181356430053,"_wandb":{"runtime":13881},"batched_nfg_sarah/val/loss":1.5362514652252197,"distributed_sarah/val/loss":1.3710181356430053,"_runtime":13881.486035083,"distributed_sarah/test/loss":1.3599584463119507,"batched_nfg_sarah/best_val_accuracy":0.4512}
\ No newline at end of file
diff --git a/wandb/run-20260413_191519-n5cnqq0l/run-n5cnqq0l.wandb b/wandb/run-20260413_191519-n5cnqq0l/run-n5cnqq0l.wandb
deleted file mode 100644
index 8f66856..0000000
Binary files a/wandb/run-20260413_191519-n5cnqq0l/run-n5cnqq0l.wandb and /dev/null differ
diff --git a/wandb/run-20260413_230641-jl7u43io/files/code/main.py b/wandb/run-20260413_230641-jl7u43io/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260413_230641-jl7u43io/files/config.yaml b/wandb/run-20260413_230641-jl7u43io/files/config.yaml
deleted file mode 100644
index 5965490..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            yrfdziy6b90ffpa2kr8sbgspaemyf34i:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115858497536"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T20:06:41.456766Z"
-                writerId: yrfdziy6b90ffpa2kr8sbgspaemyf34i
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 18
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.005
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260413_230641-jl7u43io/files/diff.patch b/wandb/run-20260413_230641-jl7u43io/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_230641-jl7u43io/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260413_230641-jl7u43io/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260413_230641-jl7u43io/files/requirements.txt b/wandb/run-20260413_230641-jl7u43io/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260413_230641-jl7u43io/files/wandb-metadata.json b/wandb/run-20260413_230641-jl7u43io/files/wandb-metadata.json
deleted file mode 100644
index 0ac80c7..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T20:06:41.456766Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115858497536"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "yrfdziy6b90ffpa2kr8sbgspaemyf34i"
-}
\ No newline at end of file
diff --git a/wandb/run-20260413_230641-jl7u43io/files/wandb-summary.json b/wandb/run-20260413_230641-jl7u43io/files/wandb-summary.json
deleted file mode 100644
index dd97f7f..0000000
--- a/wandb/run-20260413_230641-jl7u43io/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/last_val_loss":1.5673868785858154,"batched_nfg_sarah/test/loss":NaN,"distributed_sarah/val/loss":1.5673868785858154,"batched_nfg_sarah/val/accuracy":0.0976,"batched_nfg_sarah/test/accuracy":0.1,"_timestamp":1.7761235870132625e+09,"distributed_sarah/test/loss":1.5500043533325196,"distributed_sarah/best_val_loss":1.5673868785858154,"batched_nfg_sarah/last_val_accuracy":0.0976,"distributed_sarah/last_val_accuracy":0.4512,"batched_nfg_sarah/val/loss":NaN,"batched_nfg_sarah/best_val_accuracy":0.425,"_runtime":12785.228730478,"distributed_sarah/best_val_accuracy":0.4512,"distributed_sarah/test/accuracy":0.4607,"_step":300,"_wandb":{"runtime":12785},"distributed_sarah/val/accuracy":0.4512,"batched_nfg_sarah/best_val_loss":1.6098761783599853}
\ No newline at end of file
diff --git a/wandb/run-20260413_230641-jl7u43io/run-jl7u43io.wandb b/wandb/run-20260413_230641-jl7u43io/run-jl7u43io.wandb
deleted file mode 100644
index 22378ea..0000000
Binary files a/wandb/run-20260413_230641-jl7u43io/run-jl7u43io.wandb and /dev/null differ
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/code/main.py b/wandb/run-20260414_023947-efsg9kn5/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/config.yaml b/wandb/run-20260414_023947-efsg9kn5/files/config.yaml
deleted file mode 100644
index 9aa3414..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            1opgmt3ounysxyo1c89427jufjemc3f2:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "115995246592"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-13T23:39:47.334351Z"
-                writerId: 1opgmt3ounysxyo1c89427jufjemc3f2
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 12
-        batched_nfg_sarah.theta: 0.02
-        distributed_sarah.lr: 0.01
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/diff.patch b/wandb/run-20260414_023947-efsg9kn5/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260414_023947-efsg9kn5/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/requirements.txt b/wandb/run-20260414_023947-efsg9kn5/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/wandb-metadata.json b/wandb/run-20260414_023947-efsg9kn5/files/wandb-metadata.json
deleted file mode 100644
index 9782762..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-13T23:39:47.334351Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "115995246592"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "1opgmt3ounysxyo1c89427jufjemc3f2"
-}
\ No newline at end of file
diff --git a/wandb/run-20260414_023947-efsg9kn5/files/wandb-summary.json b/wandb/run-20260414_023947-efsg9kn5/files/wandb-summary.json
deleted file mode 100644
index 5140743..0000000
--- a/wandb/run-20260414_023947-efsg9kn5/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/accuracy":0.52,"batched_nfg_sarah/val/accuracy":0.6038,"batched_nfg_sarah/test/accuracy":0.6083,"distributed_sarah/best_val_accuracy":0.5214,"batched_nfg_sarah/last_val_loss":1.1205239492416381,"_step":300,"distributed_sarah/test/accuracy":0.5282,"batched_nfg_sarah/test/loss":1.1076150307655335,"distributed_sarah/last_val_loss":1.357372155380249,"batched_nfg_sarah/last_val_accuracy":0.6038,"distributed_sarah/best_val_loss":1.3547090478897095,"_timestamp":1.7761358150094879e+09,"distributed_sarah/test/loss":1.344909436225891,"distributed_sarah/last_val_accuracy":0.52,"distributed_sarah/val/loss":1.357372155380249,"batched_nfg_sarah/best_val_loss":1.1205239492416381,"_wandb":{"runtime":12227},"_runtime":12227.384574347,"batched_nfg_sarah/val/loss":1.1205239492416381,"batched_nfg_sarah/best_val_accuracy":0.6038}
\ No newline at end of file
diff --git a/wandb/run-20260414_023947-efsg9kn5/run-efsg9kn5.wandb b/wandb/run-20260414_023947-efsg9kn5/run-efsg9kn5.wandb
deleted file mode 100644
index 284145f..0000000
Binary files a/wandb/run-20260414_023947-efsg9kn5/run-efsg9kn5.wandb and /dev/null differ
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/code/main.py b/wandb/run-20260414_060335-zrl9ofrk/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/config.yaml b/wandb/run-20260414_060335-zrl9ofrk/files/config.yaml
deleted file mode 100644
index 642e9e4..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            1h5a0364efbr4pr0o36lc2r84tfaejvc:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116130152448"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-14T03:03:35.294314Z"
-                writerId: 1h5a0364efbr4pr0o36lc2r84tfaejvc
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.005
-        batched_nfg_sarah.prox_num_steps: 18
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.02
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/diff.patch b/wandb/run-20260414_060335-zrl9ofrk/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260414_060335-zrl9ofrk/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/requirements.txt b/wandb/run-20260414_060335-zrl9ofrk/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/wandb-metadata.json b/wandb/run-20260414_060335-zrl9ofrk/files/wandb-metadata.json
deleted file mode 100644
index 10fd40a..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-14T03:03:35.294314Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116130152448"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "1h5a0364efbr4pr0o36lc2r84tfaejvc"
-}
\ No newline at end of file
diff --git a/wandb/run-20260414_060335-zrl9ofrk/files/wandb-summary.json b/wandb/run-20260414_060335-zrl9ofrk/files/wandb-summary.json
deleted file mode 100644
index b167ef7..0000000
--- a/wandb/run-20260414_060335-zrl9ofrk/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"distributed_sarah/val/accuracy":0.571,"_step":300,"batched_nfg_sarah/val/accuracy":0.5318,"batched_nfg_sarah/test/loss":1.2994867038726807,"distributed_sarah/test/loss":1.1892823900222778,"batched_nfg_sarah/last_val_accuracy":0.5318,"distributed_sarah/val/loss":1.2040951715469361,"distributed_sarah/best_val_accuracy":0.5888,"batched_nfg_sarah/best_val_accuracy":0.5372,"distributed_sarah/last_val_accuracy":0.571,"_timestamp":1.7761483419403672e+09,"batched_nfg_sarah/val/loss":1.3220958909988403,"_runtime":12526.347199131,"distributed_sarah/test/accuracy":0.5802,"_wandb":{"runtime":12526},"batched_nfg_sarah/best_val_loss":1.3044685514450074,"distributed_sarah/last_val_loss":1.2040951715469361,"batched_nfg_sarah/test/accuracy":0.543,"distributed_sarah/best_val_loss":1.166837780570984,"batched_nfg_sarah/last_val_loss":1.3220958909988403}
\ No newline at end of file
diff --git a/wandb/run-20260414_060335-zrl9ofrk/run-zrl9ofrk.wandb b/wandb/run-20260414_060335-zrl9ofrk/run-zrl9ofrk.wandb
deleted file mode 100644
index d379092..0000000
Binary files a/wandb/run-20260414_060335-zrl9ofrk/run-zrl9ofrk.wandb and /dev/null differ
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/code/main.py b/wandb/run-20260414_093222-lt86lqrz/files/code/main.py
deleted file mode 100644
index 9eea11c..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/code/main.py
+++ /dev/null
@@ -1,23 +0,0 @@
-"""Entry point — run experiments via Hydra."""
-
-from __future__ import annotations
-
-import logging
-
-import hydra
-from omegaconf import DictConfig
-
-from similarity_sarah.runtime.runner import Runner
-
-logger = logging.getLogger(__name__)
-
-
-@hydra.main(config_path="configs", config_name="config", version_base=None)
-def main(cfg: DictConfig) -> None:
-    logger.info("=== Experiment config ===\n%s", cfg)
-    runner = Runner(cfg)
-    runner.run()
-
-
-if __name__ == "__main__":
-    main()
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/config.yaml b/wandb/run-20260414_093222-lt86lqrz/files/config.yaml
deleted file mode 100644
index 3c976a6..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/config.yaml
+++ /dev/null
@@ -1,169 +0,0 @@
-_wandb:
-    value:
-        cli_version: 0.25.1
-        code_path: code/main.py
-        e:
-            gy3m6h812e1i67za4pklcy3bw1umsckr:
-                args:
-                    - search=optuna
-                    - runtime.wandb.enabled=true
-                codePath: main.py
-                codePathLocal: main.py
-                cpu_count: 48
-                cpu_count_logical: 48
-                cudaVersion: "12.9"
-                disk:
-                    /:
-                        total: "142597365760"
-                        used: "116247236608"
-                email: chirkov.gd@phystech.edu
-                executable: /home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python
-                git:
-                    commit: f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1
-                    remote: git@github.com:Vladislav-Pov/similarity-sarah.git
-                gpu: NVIDIA A100-PCIE-40GB
-                gpu_count: 8
-                gpu_nvidia:
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "85899345920"
-                      name: NVIDIA A100-SXM4-80GB
-                      uuid: GPU-91279309-1b26-7c66-694f-a18d2c81fd85
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8
-                    - architecture: Ampere
-                      cudaCores: 6912
-                      memoryTotal: "42949672960"
-                      name: NVIDIA A100-PCIE-40GB
-                      uuid: GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94
-                    - architecture: Turing
-                      cudaCores: 4352
-                      memoryTotal: "11811160064"
-                      name: NVIDIA GeForce RTX 2080 Ti
-                      uuid: GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b
-                host: opt-mipt
-                memory:
-                    total: "541031403520"
-                os: Linux-5.15.0-139-generic-x86_64-with-glibc2.31
-                program: /home/Xattta6bI4/similarity-sarah/main.py
-                python: CPython 3.10.20
-                root: /home/Xattta6bI4/similarity-sarah
-                startedAt: "2026-04-14T06:32:22.213208Z"
-                writerId: gy3m6h812e1i67za4pklcy3bw1umsckr
-        m: []
-        python_version: 3.10.20
-        t:
-            "1":
-                - 1
-                - 35
-                - 41
-                - 50
-            "2":
-                - 1
-                - 35
-                - 41
-                - 50
-            "3":
-                - 2
-                - 13
-                - 15
-                - 16
-                - 61
-                - 62
-            "4": 3.10.20
-            "5": 0.25.1
-            "12": 0.25.1
-            "13": linux-x86_64
-base:
-    value:
-        algorithm:
-            batch_size_clients: 2
-            name: batched_nfg_sarah
-            num_clients: 10
-            num_epochs: 75
-            prox_lr: 0.01
-            prox_num_steps: 8
-            theta: 0.01
-        data:
-            data_dir: ./data
-            name: cifar10
-            val_fraction: 0.1
-        model:
-            name: simple_cnn
-            num_classes: 10
-        partition:
-            name: uniform
-        runtime:
-            batch_size: 128
-            device: auto
-            eval_every: 1
-            log_every: 1
-            num_workers: 2
-            wandb:
-                enabled: true
-                entity: chirkov
-                group: null
-                job_type: null
-                name: null
-                project: similarity-sarah
-                tags: []
-        search:
-            batched_nfg_sarah:
-                prox_lr:
-                    - 0.005
-                    - 0.01
-                prox_num_steps:
-                    - 8
-                    - 12
-                    - 18
-                theta:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            distributed_sarah:
-                lr:
-                    - 0.005
-                    - 0.01
-                    - 0.02
-            enabled: true
-            max_epochs: 150
-            method: optuna
-            num_trials: 10
-            shared:
-                batch_size_clients:
-                    - 1
-                num_epochs: 150
-            wandb_group: optuna-20260413-012704
-            wandb_project: similarity-sarah
-        seed: 42
-search_params:
-    value:
-        batched_nfg_sarah.prox_lr: 0.01
-        batched_nfg_sarah.prox_num_steps: 12
-        batched_nfg_sarah.theta: 0.01
-        distributed_sarah.lr: 0.005
-        shared.batch_size_clients: 1
-        shared.num_epochs: 150
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/diff.patch b/wandb/run-20260414_093222-lt86lqrz/files/diff.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/diff.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch b/wandb/run-20260414_093222-lt86lqrz/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
deleted file mode 100644
index 8235140..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/diff_f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1.patch
+++ /dev/null
@@ -1,394 +0,0 @@
-diff --git a/configs/algorithm/batched_nfg_sarah.yaml b/configs/algorithm/batched_nfg_sarah.yaml
-index 184e6b7..e2730f3 100644
---- a/configs/algorithm/batched_nfg_sarah.yaml
-+++ b/configs/algorithm/batched_nfg_sarah.yaml
-@@ -3,8 +3,8 @@ name: batched_nfg_sarah
- num_clients: 10
- batch_size_clients: 2
- theta: 0.01
--num_epochs: 50
-+num_epochs: 75
- 
- # Inexact proximal solver hyper-parameters
--prox_num_steps: 5
-+prox_num_steps: 8 # SGD steps per prox call
- prox_lr: 0.01
-diff --git a/configs/algorithm/distributed_sarah.yaml b/configs/algorithm/distributed_sarah.yaml
-index 39bd437..990ed23 100644
---- a/configs/algorithm/distributed_sarah.yaml
-+++ b/configs/algorithm/distributed_sarah.yaml
-@@ -3,4 +3,4 @@ name: distributed_sarah
- num_clients: 10
- batch_size_clients: 2
- lr: 0.01
--num_epochs: 50
-+num_epochs: 75
-diff --git a/configs/config.yaml b/configs/config.yaml
-index 7b5d68a..fa6deef 100644
---- a/configs/config.yaml
-+++ b/configs/config.yaml
-@@ -4,6 +4,7 @@ defaults:
-   - model: simple_cnn
-   - partition: uniform
-   - runtime: default
-+  - search: none
-   - _self_
- 
- seed: 42
-diff --git a/configs/runtime/default.yaml b/configs/runtime/default.yaml
-index 1e676d9..61ea13b 100644
---- a/configs/runtime/default.yaml
-+++ b/configs/runtime/default.yaml
-@@ -3,3 +3,12 @@ eval_every: 1
- log_every: 1
- num_workers: 2
- batch_size: 128
-+
-+wandb:
-+  enabled: false
-+  project: similarity-sarah
-+  entity: chirkov
-+  group: null
-+  tags: []
-+  job_type: null
-+  name: null
-diff --git a/requirements.txt b/requirements.txt
-index 003e029..7dddad1 100644
---- a/requirements.txt
-+++ b/requirements.txt
-@@ -1,5 +1,11 @@
--torch>=2.0.0
--torchvision>=0.15.0
-+# PyPI не отдаёт версии вида torch==X.Y.Z+cuNN; `pip install -r` без --index-url смотрит только PyPI.
-+# GPU: ставьте torch/torchvision с https://pytorch.org — cu130 (CUDA 13) нужен драйвер с поддержкой CUDA 13;
-+# при max CUDA 12.x в nvidia-smi используйте cu128, например:
-+#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-+torch==2.11.0
-+torchvision==0.26.0
- hydra-core>=1.3.0
- omegaconf>=2.3.0
- pytest>=7.0.0
-+wandb
-+optuna
-diff --git a/similarity_sarah/algorithms/batched_nfg_sarah.py b/similarity_sarah/algorithms/batched_nfg_sarah.py
-index e945aac..410a897 100644
---- a/similarity_sarah/algorithms/batched_nfg_sarah.py
-+++ b/similarity_sarah/algorithms/batched_nfg_sarah.py
-@@ -80,7 +80,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         self.loss_fn = loss_fn
-         self.device = device
-         self.num_clients = len(client_loaders)
--        self.total_nodes = self.num_clients + 1
-+        self.total_nodes = self.num_clients + 1 # maybe without +1
- 
-         self.scheduler = ClientBatchScheduler(
-             self.num_clients, self.batch_size_clients,
-@@ -102,7 +102,7 @@ class BatchedNoFullGradSARAH(BaseAlgorithm):
-         w_prev = get_params(self.model)
-         grad_f1_prev = compute_full_gradient(
-             self.model, self.server_loader, self.loss_fn, self.device,
--        )
-+        ) ## it is possible to count with stochastic gradient 
- 
-         # w₁ = prox_{θf₁}(w₀ − θv₀)
-         self.prox_solver.step(
-diff --git a/similarity_sarah/runtime/runner.py b/similarity_sarah/runtime/runner.py
-index f6f73f4..a5d8984 100644
---- a/similarity_sarah/runtime/runner.py
-+++ b/similarity_sarah/runtime/runner.py
-@@ -4,6 +4,9 @@ from __future__ import annotations
- 
- import logging
- import time
-+from typing import Mapping
-+
-+from omegaconf import OmegaConf
- 
- import torch
- from omegaconf import DictConfig
-@@ -16,11 +19,66 @@ from similarity_sarah.data.datasets import load_dataset, split_train_val
- from similarity_sarah.data.partition import create_partition
- from similarity_sarah.models.simple_cnn import SimpleCNN
- from similarity_sarah.runtime.prox_solver import InexactProxSGD
-+from similarity_sarah.runtime.metrics_logger import MetricsLogger, WandbLogger
- from similarity_sarah.tasks.classification import ClassificationTask
- from similarity_sarah.utils import set_seed
- 
- logger = logging.getLogger(__name__)
- 
-+# Short labels for W&B run names / tags (keys = Hydra search param paths).
-+_WANDB_PARAM_ABBREV: dict[str, str] = {
-+    "shared.batch_size_clients": "B",
-+    "shared.num_epochs": "E",
-+    "distributed_sarah.lr": "dlr",
-+    "batched_nfg_sarah.theta": "th",
-+    "batched_nfg_sarah.prox_lr": "plr",
-+    "batched_nfg_sarah.prox_num_steps": "pns",
-+}
-+
-+
-+def _wandb_abbrev_param_key(key: str) -> str:
-+    if key in _WANDB_PARAM_ABBREV:
-+        return _WANDB_PARAM_ABBREV[key]
-+    tail = key.split(".")[-1]
-+    return tail[:16] if len(tail) > 16 else tail
-+
-+
-+def _format_param_value_for_wandb(value: object) -> str:
-+    if isinstance(value, float):
-+        return f"{value:g}"
-+    return str(value)
-+
-+
-+def _wandb_trial_run_name(trial_id: int, trial_params: Mapping[str, object]) -> str:
-+    """Build a readable W&B run name: trial index + compact hyperparameter summary."""
-+    parts: list[str] = [f"t{trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        parts.append(f"{abbrev}={val}")
-+    raw = "__".join(parts)
-+    safe = "".join(c if c.isalnum() or c in "-_=." else "_" for c in raw)
-+    max_len = 128
-+    if len(safe) > max_len:
-+        safe = safe[: max_len - 3] + "..."
-+    return safe
-+
-+
-+def _wandb_trial_param_tags(
-+    trial_id: int,
-+    trial_params: Mapping[str, object],
-+) -> list[str]:
-+    """One tag per hyperparameter (and trial id) for W&B table filters."""
-+    tags: list[str] = [f"trial_id={trial_id}"]
-+    for key in sorted(trial_params.keys()):
-+        abbrev = _wandb_abbrev_param_key(key)
-+        val = _format_param_value_for_wandb(trial_params[key])
-+        tag = f"{abbrev}={val}"
-+        if len(tag) > 64:
-+            tag = tag[:61] + "..."
-+        tags.append(tag)
-+    return tags
-+
- 
- class Runner:
-     """Top-level object that sets up and executes an experiment."""
-@@ -31,9 +89,9 @@ class Runner:
- 
-         self._setup_device()
-         self._setup_data()
--        self._setup_model()
-         self._setup_task()
--        self._setup_algorithm()
-+
-+        self._logger: MetricsLogger | None = None
- 
-     # ------------------------------------------------------------------
-     # Setup helpers
-@@ -99,8 +157,8 @@ class Runner:
-     def _setup_task(self) -> None:
-         self.task = ClassificationTask()
- 
--    def _setup_algorithm(self) -> None:
--        algo_cfg = self.cfg.algorithm
-+    def _setup_algorithm(self, cfg: DictConfig) -> None:
-+        algo_cfg = cfg.algorithm
-         algorithm: BaseAlgorithm
- 
-         if algo_cfg.name == "batched_nfg_sarah":
-@@ -135,11 +193,109 @@ class Runner:
-     # Training loop
-     # ------------------------------------------------------------------
-     def run(self) -> None:
--        num_epochs: int = self.cfg.algorithm.num_epochs
--        eval_every: int = self.cfg.runtime.eval_every
-+        search_cfg = self.cfg.search
-+        if not getattr(search_cfg, "enabled", False):
-+            self._setup_model()
-+            self._setup_algorithm(self.cfg)
-+            self._logger = self._maybe_create_logger(self.cfg, run_name=None)
-+            try:
-+                self._run_training(
-+                    self.cfg,
-+                    algo_name=self.cfg.algorithm.name,
-+                    run_name=self.cfg.algorithm.name,
-+                    global_step_offset=0,
-+                )
-+            finally:
-+                if self._logger is not None:
-+                    self._logger.finish()
-+            return
-+
-+        self._run_search(search_cfg)
-+
-+    # ------------------------------------------------------------------
-+    # Search / orchestration
-+    # ------------------------------------------------------------------
-+    def _run_search(self, search_cfg: DictConfig) -> None:
-+        method = str(search_cfg.method).lower()
-+        if method not in {"grid", "optuna"}:
-+            raise ValueError(f"Unknown search method: {search_cfg.method}")
-+
-+        if method == "grid":
-+            from similarity_sarah.runtime.search import GridSearch
-+
-+            searcher = GridSearch(search_cfg, self.cfg)
-+        else:
-+            from similarity_sarah.runtime.search import OptunaSearch
-+
-+            searcher = OptunaSearch(search_cfg, self.cfg)
-+
-+        best = searcher.run(self._evaluate_trial)
-+
-+        logger.info("Search done. Best summary: %s", best)
-+
-+    def _evaluate_trial(
-+        self,
-+        *,
-+        trial_id: int,
-+        trial_cfgs: dict[str, DictConfig],
-+        trial_params: dict[str, object],
-+    ) -> dict[str, object]:
-+        result: dict[str, object] = {"trial_id": trial_id, "params": trial_params}
-+        wandb_run_name = _wandb_trial_run_name(trial_id, trial_params)
-+        trial_tags = _wandb_trial_param_tags(trial_id, trial_params)
-+        logger.info("Search trial %s — W&B run name: %s", trial_id, wandb_run_name)
-+
-+        search_cfg = self.cfg.search
-+        self._logger = self._maybe_create_logger(
-+            self.cfg,
-+            run_name=wandb_run_name,
-+            project_override=search_cfg.get("wandb_project"),
-+            group_override=search_cfg.get("wandb_group"),
-+            config_override=trial_params,
-+            extra_tags=trial_tags,
-+        )
-+
-+        try:
-+            summaries = {}
-+            wandb_step_offset = 0
-+            for algo_name, algo_cfg in trial_cfgs.items():
-+                self._setup_model()
-+                self._setup_algorithm(algo_cfg)
-+                summary = self._run_training(
-+                    algo_cfg,
-+                    algo_name=algo_name,
-+                    run_name=f"{wandb_run_name}/{algo_name}",
-+                    global_step_offset=wandb_step_offset,
-+                )
-+                summaries[algo_name] = summary
-+                wandb_step_offset += int(algo_cfg.algorithm.num_epochs)
-+            result["summaries"] = summaries
-+            return result
-+        finally:
-+            if self._logger is not None:
-+                self._logger.finish()
-+            self._logger = None
-+
-+    # ------------------------------------------------------------------
-+    # Training loop
-+    # ------------------------------------------------------------------
-+    def _run_training(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        algo_name: str,
-+        run_name: str,
-+        global_step_offset: int,
-+    ) -> dict[str, float]:
-+        num_epochs: int = cfg.algorithm.num_epochs
-+        eval_every: int = cfg.runtime.eval_every
- 
-         logger.info("Starting training for %d epochs", num_epochs)
- 
-+        best_val_acc = -float("inf")
-+        best_val_loss = float("inf")
-+        last_val = None
-+
-         for epoch in range(num_epochs):
-             t0 = time.perf_counter()
-             metrics = self.algorithm.run_epoch(epoch)
-@@ -159,3 +315,83 @@ class Runner:
-                 )
-                 logger.info("  val:  %s", val_metrics)
-                 logger.info("  test: %s", test_metrics)
-+
-+                if self._logger is not None:
-+                    step = global_step_offset + epoch + 1
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="val",
-+                        metrics=val_metrics,
-+                        step=step,
-+                    )
-+                    self._logger.log(
-+                        algo=algo_name,
-+                        split="test",
-+                        metrics=test_metrics,
-+                        step=step,
-+                    )
-+
-+                last_val = val_metrics
-+                best_val_acc = max(best_val_acc, val_metrics.get("accuracy", -1))
-+                best_val_loss = min(best_val_loss, val_metrics.get("loss", float("inf")))
-+
-+        summary = {
-+            "best_val_accuracy": best_val_acc,
-+            "best_val_loss": best_val_loss,
-+        }
-+        if last_val is not None:
-+            summary["last_val_accuracy"] = last_val.get("accuracy")
-+            summary["last_val_loss"] = last_val.get("loss")
-+
-+        if self._logger is not None:
-+            self._logger.log_summary(algo=algo_name, metrics=summary)
-+
-+        return summary
-+
-+    # ------------------------------------------------------------------
-+    # W&B logging helpers
-+    # ------------------------------------------------------------------
-+    def _maybe_create_logger(
-+        self,
-+        cfg: DictConfig,
-+        *,
-+        run_name: str | None = None,
-+        project_override: str | None = None,
-+        group_override: str | None = None,
-+        config_override: dict[str, object] | None = None,
-+        extra_tags: list[str] | None = None,
-+    ) -> MetricsLogger | None:
-+        wandb_cfg = cfg.runtime.get("wandb")
-+        if not wandb_cfg or not wandb_cfg.get("enabled", False):
-+            return None
-+
-+        project = project_override or wandb_cfg.get("project", "similarity-sarah")
-+        entity = wandb_cfg.get("entity")
-+        group = group_override or wandb_cfg.get("group")
-+        tags = list(wandb_cfg.get("tags") or [])
-+        if extra_tags:
-+            tags.extend(extra_tags)
-+        job_type = wandb_cfg.get("job_type")
-+        name = run_name or wandb_cfg.get("name")
-+        run_config = OmegaConf.to_container(cfg, resolve=True)
-+        if config_override:
-+            run_config = {"base": run_config, "search_params": config_override}
-+
-+        notes: str | None = None
-+        if config_override:
-+            lines = [
-+                f"{k}: {_format_param_value_for_wandb(v)}"
-+                for k, v in sorted(config_override.items())
-+            ]
-+            notes = "Search trial hyperparameters:\n" + "\n".join(lines)
-+
-+        return WandbLogger.start(
-+            project=project,
-+            entity=entity,
-+            group=group,
-+            tags=tags,
-+            job_type=job_type,
-+            name=name,
-+            notes=notes,
-+            config=run_config,
-+        )
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/requirements.txt b/wandb/run-20260414_093222-lt86lqrz/files/requirements.txt
deleted file mode 100644
index 9126521..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/requirements.txt
+++ /dev/null
@@ -1,85 +0,0 @@
-nvidia-cusparselt-cu12==0.7.1
-charset-normalizer==3.4.7
-pluggy==1.6.0
-greenlet==3.4.0
-nvidia-cusolver==12.0.4.66
-Jinja2==3.1.6
-mpmath==1.3.0
-torch==2.11.0+cu128
-nvidia-cusparse==12.6.3.3
-hydra-core==1.3.2
-tqdm==4.67.3
-nvidia-nvtx==13.0.85
-nvidia-cufile==1.15.1.6
-nvidia-cuda-runtime==13.0.96
-optuna==4.8.0
-GitPython==3.1.46
-nvidia-nvjitlink-cu12==12.8.93
-nvidia-cusparse-cu12==12.5.8.93
-sympy==1.14.0
-urllib3==2.6.3
-nvidia-cusparselt-cu13==0.8.0
-iniconfig==2.3.0
-gitdb==4.0.12
-fsspec==2026.2.0
-packaging==26.0
-requests==2.33.1
-omegaconf==2.3.0
-filelock==3.25.2
-Pygments==2.20.0
-nvidia-nvjitlink==13.0.88
-typing_extensions==4.15.0
-pip==26.0.1
-triton==3.6.0
-typing-inspection==0.4.2
-nvidia-nvtx-cu12==12.8.90
-Mako==1.3.10
-cuda-bindings==12.9.4
-numpy==2.2.6
-MarkupSafe==3.0.3
-nvidia-cudnn-cu12==9.19.0.56
-PyYAML==6.0.3
-nvidia-cuda-cupti-cu12==12.8.90
-networkx==3.4.2
-pydantic_core==2.41.5
-colorlog==6.10.1
-SQLAlchemy==2.0.49
-click==8.3.2
-sentry-sdk==2.57.0
-nvidia-cublas-cu12==12.8.4.1
-torchvision==0.26.0+cu128
-nvidia-nccl-cu12==2.28.9
-pytest==9.0.3
-idna==3.11
-cuda-toolkit==12.8.1
-nvidia-cufft==12.0.0.61
-nvidia-curand-cu12==10.3.9.90
-smmap==5.0.3
-platformdirs==4.9.6
-setuptools==70.2.0
-nvidia-nccl-cu13==2.28.9
-wandb==0.25.1
-nvidia-cufft-cu12==11.3.3.83
-torchaudio==2.11.0+cu129
-nvidia-cublas==13.1.0.3
-tomli==2.4.1
-nvidia-cuda-nvrtc==13.0.88
-nvidia-cuda-nvrtc-cu12==12.8.93
-nvidia-cuda-runtime-cu12==12.8.90
-antlr4-python3-runtime==4.9.3
-pydantic==2.12.5
-certifi==2026.2.25
-nvidia-nvshmem-cu12==3.4.5
-annotated-types==0.7.0
-nvidia-cusolver-cu12==11.7.3.90
-nvidia-curand==10.4.0.35
-nvidia-cudnn-cu13==9.19.0.56
-nvidia-cuda-cupti==13.0.85
-wheel==0.46.3
-protobuf==6.33.6
-pillow==12.1.1
-nvidia-nvshmem-cu13==3.4.5
-alembic==1.18.4
-nvidia-cufile-cu12==1.13.1.3
-exceptiongroup==1.3.1
-cuda-pathfinder==1.2.2
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/wandb-metadata.json b/wandb/run-20260414_093222-lt86lqrz/files/wandb-metadata.json
deleted file mode 100644
index afc5626..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/wandb-metadata.json
+++ /dev/null
@@ -1,93 +0,0 @@
-{
-  "os": "Linux-5.15.0-139-generic-x86_64-with-glibc2.31",
-  "python": "CPython 3.10.20",
-  "startedAt": "2026-04-14T06:32:22.213208Z",
-  "args": [
-    "search=optuna",
-    "runtime.wandb.enabled=true"
-  ],
-  "program": "/home/Xattta6bI4/similarity-sarah/main.py",
-  "codePath": "main.py",
-  "codePathLocal": "main.py",
-  "git": {
-    "remote": "git@github.com:Vladislav-Pov/similarity-sarah.git",
-    "commit": "f9a7ebe5d85e96c5275ffe6a095ab4e6ca56acb1"
-  },
-  "email": "chirkov.gd@phystech.edu",
-  "root": "/home/Xattta6bI4/similarity-sarah",
-  "host": "opt-mipt",
-  "executable": "/home/Xattta6bI4/miniconda3/envs/nfg_env/bin/python",
-  "cpu_count": 48,
-  "cpu_count_logical": 48,
-  "gpu": "NVIDIA A100-PCIE-40GB",
-  "gpu_count": 8,
-  "disk": {
-    "/": {
-      "total": "142597365760",
-      "used": "116247236608"
-    }
-  },
-  "memory": {
-    "total": "541031403520"
-  },
-  "gpu_nvidia": [
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-65642e9c-aa01-6b54-9940-e7d03971b2f6"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-c6d16bdc-c5c9-e35b-4e92-8c96988384f1"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-093c0e0c-a9ee-3383-ac6d-e6b3d6095c1b"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-7a50f6c7-f545-0b4c-f32f-9edb6a7e7eaa"
-    },
-    {
-      "name": "NVIDIA A100-SXM4-80GB",
-      "memoryTotal": "85899345920",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-91279309-1b26-7c66-694f-a18d2c81fd85"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-5430d3bb-f055-7a03-62f9-36ce1cf238c8"
-    },
-    {
-      "name": "NVIDIA A100-PCIE-40GB",
-      "memoryTotal": "42949672960",
-      "cudaCores": 6912,
-      "architecture": "Ampere",
-      "uuid": "GPU-b2f68da5-f46c-c411-5fca-74699a6a3f94"
-    },
-    {
-      "name": "NVIDIA GeForce RTX 2080 Ti",
-      "memoryTotal": "11811160064",
-      "cudaCores": 4352,
-      "architecture": "Turing",
-      "uuid": "GPU-d9fc0582-a134-ded0-909d-e88d902ecc0b"
-    }
-  ],
-  "cudaVersion": "12.9",
-  "writerId": "gy3m6h812e1i67za4pklcy3bw1umsckr"
-}
\ No newline at end of file
diff --git a/wandb/run-20260414_093222-lt86lqrz/files/wandb-summary.json b/wandb/run-20260414_093222-lt86lqrz/files/wandb-summary.json
deleted file mode 100644
index 2a7aef4..0000000
--- a/wandb/run-20260414_093222-lt86lqrz/files/wandb-summary.json
+++ /dev/null
@@ -1 +0,0 @@
-{"batched_nfg_sarah/test/loss":NaN,"batched_nfg_sarah/test/accuracy":0.1,"distributed_sarah/test/loss":1.5773095851898193,"distributed_sarah/val/loss":1.594420343017578,"distributed_sarah/last_val_loss":1.594420343017578,"batched_nfg_sarah/best_val_loss":1.6248399473190307,"batched_nfg_sarah/best_val_accuracy":0.4262,"batched_nfg_sarah/last_val_accuracy":0.0976,"_wandb":{"runtime":11246},"distributed_sarah/best_val_accuracy":0.4346,"distributed_sarah/val/accuracy":0.4346,"distributed_sarah/best_val_loss":1.594420343017578,"batched_nfg_sarah/val/accuracy":0.0976,"batched_nfg_sarah/val/loss":NaN,"distributed_sarah/last_val_accuracy":0.4346,"distributed_sarah/test/accuracy":0.4456,"_runtime":11246.578966603,"_step":300,"_timestamp":1.7761595890797153e+09}
\ No newline at end of file
diff --git a/wandb/run-20260414_093222-lt86lqrz/run-lt86lqrz.wandb b/wandb/run-20260414_093222-lt86lqrz/run-lt86lqrz.wandb
deleted file mode 100644
index 9d9fafc..0000000
Binary files a/wandb/run-20260414_093222-lt86lqrz/run-lt86lqrz.wandb and /dev/null differ
